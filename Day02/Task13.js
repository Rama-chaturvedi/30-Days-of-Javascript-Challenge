// Example conditions
let condition1 = true;
let condition2 = false;

// Using the ! operator to combine conditions
if (!(condition1 && condition2)) {
    console.log("At least one condition is false");
} else {
    console.log("Both conditions are true");
}